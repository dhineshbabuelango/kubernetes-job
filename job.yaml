apiVersion: v1
kind: Pod
metadata:
  labels:
    infra: docker
spec:
  containers:
  - name: docker
    image: docker
    command:
    - cat
    tty: true
    volumeMounts:
    - name: dockersock
      mountPath: /var/run/docker.sock
  volumes:
  - name: dockersock
    hostPath:
      path: /var/run/docker.sock
  - name: kubectl
    image: dineshelango/utils:v1
    command:
    - cat
    tty: true